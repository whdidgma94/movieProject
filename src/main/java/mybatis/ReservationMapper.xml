<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.reservation">

	<resultMap id="reservation" type="Reservation.ReservationVO">
		<id property="reserveNo" column="reserveNo" />
		<result property="memberId" column="memberId" />
		<result property="screeningNo" column="screeningNo" />
		<result property="seatNumber" column="seatNumber" />
	</resultMap>

	<select id="getAllReservation" resultMap="reservation">
		SELECT * FROM reservation
	</select>

	<select id="getMemberReservation" parameterType="String" resultType="reservation">
		select * from reservation where memberId=#{memberId}
	</select>

	<select id="getOneReservation" resultType="reservation" parameterType="Integer">
		select * from user where reserveNo=#{reserveNo}
	</select>


	<insert id="insertReservation" parameterType="reservation">
		INSERT INTO reservation (memberId, screeningNo, seatNumber)
		VALUES(
		#{memberId},
		#{screeningNo},
		#{seatNumber}
		)
	</insert>

	<delete id="deleteReservation" parameterType="Integer">
		delete from reservation where reservationNo=#{reservationNo}
	</delete>

</mapper>